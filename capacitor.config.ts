import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.silambam.master',
  appName: 'Silambam Master Suite',
  webDir: 'dist/capacitor'
};

export default config;
